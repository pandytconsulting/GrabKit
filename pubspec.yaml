name: grabkit
description: Core runtime and module contracts for the modular GrabKit Flutter diagnostic toolkit.
version: 0.1.0
homepage: https://github.com/pandytconsulting/GrabKit
repository: https://github.com/pandytconsulting/GrabKit
issue_tracker: https://github.com/pandytconsulting/GrabKit/issues
topics:
  - debugging
  - devtools
  - dio
  - logging
  - qa

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
